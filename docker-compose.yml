version: "3.0"


services:


  minio:
    image: 'bitnami/minio:latest'
    ports:
      - '9090:9090'
    environment:
      - MINIO_ROOT_USER=admin
      - MINIO_ROOT_PASSWORD=admin
    networks:
      - mlops






networks:
  mlops:
    driver: bridge

